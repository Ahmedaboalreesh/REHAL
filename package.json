{
  "name": "rehal-platform",
  "version": "1.0.0",
  "description": "منصة رحال - منصة النقل الآمن للطالبات والعاملات",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "init:db": "node -e \"(async()=>{const {Pool}=require('pg');const fs=require('fs');require('dotenv').config();const pool=new Pool({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}});const sql=fs.readFileSync('schema.sql','utf8');await pool.query(sql);await pool.end();console.log('Schema applied');})().catch(e=>{console.error(e);process.exit(1)})\"",
    "build": "echo No build step",
    "test": "echo No tests"
  },
  "keywords": [
    "transportation",
    "women",
    "saudi-arabia",
    "microbus",
    "booking",
    "arabic"
  ],
  "author": "REHAL Team",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/rehal-platform.git"
  },
  "bugs": {
    "url": "https://github.com/your-username/rehal-platform/issues"
  },
  "homepage": "https://github.com/your-username/rehal-platform#readme"
}
